import { uikitConfigurationReducer } from '../reducer';

describe('uikitConfigurationReducer', () => {
  it('should return the initial state', () => {
    expect(1 + 1).toEqual(fromJS({}));
  });
});
