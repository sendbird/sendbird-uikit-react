const IMAGE_GRID_WIDTH = 400;
const PADDING = 4;
const GAP = 4;
const MESSAGE_CONTENT_SIDE_PADDING = 28;
const MARGIN_LEFT = 12;
const PROFILE_IMAGE_WIDTH = 40;
const THREAD_WIDTH = 320;
const CHAT_BODY_SIDE_PADDING = 24;

const THREAD_CHILD_WIDTH = 200;
const THREAD_CHILD_SIDE_PADDING = 8

const THREAD_CHILD_MOBILE_SIDE_PADDING = 32;
const THREAD_CHILD_MOBILE_TIMESTAMP_WIDTH_AND_ITS_SIDE_MARGIN = 60;

export const MULTIPLE_FILES_IMAGE_SIDE_LENGTH = {
  CHAT_WEB: `calc(${IMAGE_GRID_WIDTH / 2}px - ${2 * (PADDING / 2)}px - ${GAP / 2}px)`,
  CHAT_MOBILE: `calc(50vw - ${CHAT_BODY_SIDE_PADDING}px - ${MESSAGE_CONTENT_SIDE_PADDING}px - ${2 * (PADDING / 2)}px - ${GAP / 2}px)`,
  THREAD_PARENT_WEB: `calc(${(THREAD_WIDTH / 2) - (MESSAGE_CONTENT_SIDE_PADDING / 2 + PROFILE_IMAGE_WIDTH / 2 + MARGIN_LEFT / 2 + GAP / 2)}px)`,
  THREAD_PARENT_MOBILE: `calc(50vw - ${MESSAGE_CONTENT_SIDE_PADDING / 2 + PROFILE_IMAGE_WIDTH / 2 + MARGIN_LEFT / 2 + GAP / 2}px)`,
  THREAD_CHILD_WEB: `calc(${(THREAD_CHILD_WIDTH - THREAD_CHILD_SIDE_PADDING - GAP) / 2}px)`,
  THREAD_CHILD_MOBILE: `calc(50vw - ${(THREAD_CHILD_MOBILE_SIDE_PADDING + PROFILE_IMAGE_WIDTH + THREAD_CHILD_MOBILE_TIMESTAMP_WIDTH_AND_ITS_SIDE_MARGIN + PADDING + GAP) / 2}px)`,
}
export const MULTIPLE_FILES_IMAGE_BORDER_RADIUS = {
  CHAT_WEB: '6px',
  CHAT_MOBILE: '6px',
  THREAD_PARENT_WEB: '6px',
  THREAD_PARENT_MOBILE: '6px',
  THREAD_CHILD_WEB: '6px',
  THREAD_CHILD_MOBILE: '6px',
}
export const MULTIPLE_FILES_IMAGE_THUMBNAIL_SIDE_LENGTH = '40px';
