import React from 'react';

export default function Introduction(): JSX.Element {
  return (
    <article>
      <h1>Welcome to <i>@sendbird/uikit-react/ui</i> Storybook</h1>
      <main>
        <p>The list of UI components can be founder under `@sendbird/uikit-react/ui` in left panel</p>
        <p>Use Docs tab on the top(beside canvas tab) to read docs and play around with the components</p>
      </main>
    </article>
  );
}
