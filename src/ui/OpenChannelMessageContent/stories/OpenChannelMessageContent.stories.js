import React from 'react';
import OpenChannelMessageContent from '../index.tsx';

export default { title: 'UI Components/OpenChannelMessageContent' };

export const withText = () => <OpenChannelMessageContent text="example" />;
